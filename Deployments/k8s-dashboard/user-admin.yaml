apiVersion: v1
kind: Secret
metadata:
  name: admin-dash
  namespace: kubernetes-dashboard
  annotations:
    kubernetes.io/service-account.name: "admin-dash"   
type: kubernetes.io/service-account-token
